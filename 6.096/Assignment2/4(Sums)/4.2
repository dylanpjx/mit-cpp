sum is an overloaded function that both returns a double value and an int value. In the case of sum(1, 10.0), the arguments passed have mixed types and the compiler doesn't know which sum function to call.
