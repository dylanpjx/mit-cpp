When the array is returned, a pointer to the first element of the array will be created. However, outside of the function, the array goes out of scope and the pointer becomes a null pointer and the function is unusable.
