a const version of get is needed to obtain a read-only pointer to PointArray to prevent any end user errors. Especially in the case of a declaration of const PointArray. The non const get is used for modification of a non const PointArray respectively.
